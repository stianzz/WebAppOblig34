@model List<Oblig3MVC.Models.Faq>

@{
    ViewBag.Title = "Faq";
}

<!DOCTYPE html>
<html>
<head>
    <title>ListKategorier</title>
</head>
<body>
    <div>
        <br />
        <br />
        <h4>Faqs:</h4><br />
        <br />

        <table>

            @foreach (var Faq in Model)
            {
                if (Faq.id == 3)
                {
                    <tr>

                        <td>


                            @Html.ActionLink(Faq.problem, "Faq1")
                        <td />

                    </tr>
                }
            }
        </table>
        <br />
        <table>
            @foreach (var Faq in Model)
            {
                if (Faq.id == 3)
                {
                    <tr>

                        <td>


                            @Html.DisplayFor(m => Faq.losning)
                        <td />

                    </tr>
                }
            }
        </table>



    </div>
</body>
</html>

